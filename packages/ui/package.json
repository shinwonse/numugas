{
  "name": "@numugas/ui",
  "version": "1.0.0",
  "sideEffects": false,
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@numugas/eslint-config": "workspace:*",
    "@numugas/prettier-config": "workspace:*",
    "@numugas/typescript-config": "workspace:*",
    "@types/react": "^18.2.74",
    "@types/react-dom": "^18.2.24",
    "tsup": "^8.0.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
